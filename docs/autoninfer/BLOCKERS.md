# Autoninfer — Blockers requiring user action

The single place the research loop records anything it cannot resolve itself and that needs
you. The loop updates and pushes this file; check it periodically on GitHub. The loop keeps
running through everything short of an unrecoverable state — at which point it stops itself,
records the reason here (and in `/tmp/autoninfer-stop`), and halts cleanly.

**Nothing here is urgent unless it says "loop stopped".** A running loop with no active
blocker needs nothing from you.

## Active blockers

| date (UTC) | blocker | needed action | state |
|---|---|---|---|
| 2026-08-18 12:50 | MTP stream residual vs pure greedy (k=0): after the committed-column bit-clone fix (e84ddaef, kept as a correctness fix, M1 +13.8%), the k=3 gate stream still differs from k=0 on 4/8 prompts within 256 tok (AIME k=1: single near-tie token flip at idx 119, 280 vs 343, reconverges 7 tok later). This exceeds the quality policy's small-diff threshold (2/8). Mechanism attributed 2026-08-18 ~12:55 by the interactive session (live WIP, uncommitted): **case (a) — residual committed-column defect in the FP8 linear small-t family** (T=2..4 kept the one-chain vpl=16 association + an A16 island over the A8 T=1 reference, while the NVFP4 family got the 4-chain T=1 clone in 5cbba58c). Bit-clone fix for T=2..4 implemented + op-level bit-parity test in progress. | Ratify keeping MTP at k=3 once the FP8 committed-column fix lands and the gate re-measures (expect the 4/8 diff to shrink; north-star speed, committed column becomes exactly the canonical greedy computation). | open — fix still in the interactive session's WIP (uncommitted as of 13:00; their layer-hash AIME dumps completed at run 158 but still show ~1095 old-vs-new layer-hash diffs, so the fix was not verified clean at completion; last interactive write 12:54:09, idle since). Driver iterations 8–16 backed off (2026-08-18 13:07: both sessions still alive and idle; WIP fingerprint `git diff | sha256sum` = `d49b4f84…bb705`, 8 files +105/−41, unchanged since 12:54:09; parent shells are children of the `herdr server` daemon, so the ttys survive user disconnect — no abandonment signal. Layer-hash dumps re-measured with the file-order (run,part,layer) join at iter 15 and again at iter 16 (13:07): 1088/1152 tuples still differ, first diff at 9th line = run=150 mix layer=4 (layers 0–3 of both mix+mlp match; refines iter 14's layer=10). Re-measure after it commits (next driver iteration's steps 3–4). **Iter 17 (2026-08-18 13:10, backoff #10): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09); layer hashes re-counted 13:09: 1088/1152 differing, first at line 9 = run=150 mix layer=4. Two clarifications: the 13:07 `/tmp/lh_{old,new}_keys.txt` files are driver iter-16's own scratch (not interactive writes — last interactive write remains 12:54:09), and the new `pi` process 240219 (13:08) is the driver's own session (parent chain `timeout … pi -p … autoninfer` → `drive.sh`), not a third interactive pi. **Iter 18 (2026-08-18 13:11, backoff #11): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09); layer hashes re-counted 13:10: 1088/1152 differing, first at line 9 = run=150 mix layer=4; last interactive write remains 12:54:09 (13:07–13:08 /tmp files are driver iter-17 scratch). **Iter 19 (2026-08-18 13:12, backoff #12): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09); layer hashes re-counted 13:11: 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:07–13:10 /tmp files are driver iters 16–18 scratch). Budget: 30 h from ~09:00 UTC → clean exit ~15:00 UTC; remaining iterations back off in docs-only mode if the sessions stay alive. **Iter 20 (2026-08-18 13:12, backoff #13): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:12); layer hashes re-counted 13:12: 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:08–13:11 /tmp files are driver iters 17–19 scratch); manual serve pid 21537 alive (up 04:52), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`. **Iter 21 (2026-08-18 13:13, backoff #14): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:13); layer hashes re-counted 13:13: 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54 (13:07–13:12 /tmp files are driver iters 16–20 scratch); manual serve pid 21537 alive (up 04:53), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 45 min to the ~15:00 UTC budget exit. **Iter 22 (2026-08-18 13:15, backoff #15): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:54:09/12:50:36 re-verified 13:14); layer hashes re-counted 13:15: 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54 (13:08–13:13 /tmp files are driver iters 17–21 scratch); manual serve pid 21537 alive (up 04:54), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 45 min to the ~15:00 UTC budget exit. **Iter 23 (2026-08-18 13:16, backoff #16): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:15); layer hashes re-counted 13:15: 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:10–13:14 /tmp files are driver iters 18–22 scratch); manual serve pid 21537 alive (up 04:55), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 44 min to the ~15:00 UTC budget exit. **Iter 24 (2026-08-18 13:17, backoff #17): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:16); layer hashes re-counted 13:16: 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:10–13:14 /tmp files are driver iters 18–23 scratch); manual serve pid 21537 alive (up 04:56), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 43 min to the ~15:00 UTC budget exit.  **Iter 26 (2026-08-18 13:18, backoff #19): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:18); layer hashes re-counted 13:18 (driver scratch `lh_*.iter26.txt`, corrected paste-join after a first-pass pipeline bug): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:16–13:17 /tmp files are driver iter-25 scratch); manual serve pid 21537 alive (up 04:58), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 40 min to the ~15:00 UTC budget exit. **Iter 27 (2026-08-18 13:20, backoff #20): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:19); layer hashes re-counted 13:19 (paste-join of the two `LH run=… h=…` extracts): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:16–13:19 /tmp files are driver iters 25–27 scratch); manual serve pid 21537 alive (up 04:59), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 40 min to the ~15:00 UTC budget exit. **Iter 28 (2026-08-18 13:21, backoff #21): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:20); layer hashes re-counted 13:20 (driver scratch `lh_*.iter28.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:19 /tmp files are driver iter-27 scratch); manual serve pid 21537 alive (up 05:00), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 39 min to the ~15:00 UTC budget exit. **Iter 29 (2026-08-18 13:22, backoff #22): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:21); layer hashes re-counted 13:22 (driver scratch `lh_*.iter29.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:20 /tmp files are driver iter-28 scratch); manual serve pid 21537 alive (up 05:01), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 38 min to the ~15:00 UTC budget exit. **Iter 30 (2026-08-18 13:24, backoff #23): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:24); layer hashes re-counted 13:24 (driver scratch `lh_*.iter30.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:22 /tmp files are driver iter-29 scratch); manual serve pid 21537 alive (up 05:02), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 36 min to the ~15:00 UTC budget exit. **Iter 31 (2026-08-18 13:25, backoff #24): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:24); layer hashes re-counted 13:24 (driver scratch `lh_*.iter31.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:22–13:23 /tmp files are driver iter-30 scratch); manual serve pid 21537 alive (up 05:03), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 35 min to the ~15:00 UTC budget exit. **Iter 32 (2026-08-18 13:25, backoff #25): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:25); layer hashes re-counted 13:25 (driver scratch `lh_*.iter32.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:24 /tmp files are driver iter-31 scratch); manual serve pid 21537 alive (up 05:04), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 35 min to the ~15:00 UTC budget exit. **Iter 33 (2026-08-18 13:26, backoff #26): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:26); layer hashes re-counted 13:26 (driver scratch `lh_*.iter33.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (only non-driver files newer than it are `lh_{old,new}_keys.txt` at 13:07:07 = driver iter-16 scratch, already documented); manual serve pid 21537 alive (up 05:05:53), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 34 min to the ~15:00 UTC budget exit. **Iter 34 (2026-08-18 13:29, backoff #27): unchanged** — both sessions alive and idle (21662 CPU 27:21 flat, 237820 0:02); WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:28); layer hashes re-counted 13:28 (driver scratch `lh_*.iter34.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:26–13:27 /tmp files are driver iter-33 scratch); manual serve pid 21537 alive (up 05:07), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 31 min to the ~15:00 UTC budget exit. **Iter 35 (2026-08-18 13:30, backoff #28): mostly unchanged, one session gone** — pid 237820 exited between 13:28 and 13:29; pid 21662 alive (pts/6, up 05:06:49, CPU 28:34 vs 27:21 — ~73 s CPU in ~5 min, stat Rl+); the tree is still dirty and owned by it, so the backoff rule still applies. WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes 12:50:36/12:54:09 re-verified 13:29); layer hashes re-counted 13:29 (driver scratch `lh_*.iter35.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (13:27 /tmp files are driver iter-34 scratch; the only non-driver files newer than it are still `lh_{old,new}_keys.txt` at 13:07:07 = driver iter-16 scratch); manual serve pid 21537 alive (up 05:09:28), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 30 min to the ~15:00 UTC budget exit. **Iter 36 (2026-08-18 13:31, backoff #29): unchanged** — pid 21662 alive and still consuming CPU (CPU 28:34→29:59, ~75 s in ~1 min; pts/6, ppid 3785 `herdr server`); pid 237820 still gone. WIP byte-identical (fingerprint `d49b4f84…bb705`, mtimes re-verified 13:31: 12:50:36.462 / 12:54:09.132/136 / 12:06:23 / 12:47:39); layer hashes re-counted 13:31 (driver scratch `lh_*.iter36.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); last interactive write remains 12:54:09 (only non-driver files newer are `lh_{old,new}_keys.txt` at 13:07:07 = driver iter-16 scratch); manual serve pid 21537 alive (up 05:10:57), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 29 min to the ~15:00 UTC budget exit. **Iter 37 (2026-08-18 13:33, backoff #30): session no longer idle — actively rebuilding the WIP.** pid 21662 alive (pts/6, CPU 29:59→31:16→31:24, climbing ~75–85 s/min); at 13:32 it launched a rebuild of the WIP tree (its child pid 245332: `git diff tests/CMakeLists.txt && cmake --build build -j`; ninja/nvcc/ptxas active; the fresh `tmpxft_*` nvcc temp files in /tmp at 13:32 are this build's artifacts, not new WIP writes). Repo WIP still byte-identical (fingerprint `d49b4f84…bb705`, last repo write 12:54:09) — i.e. a rebuild of the *same* WIP, presumably to re-run the parity test; a new edit may land at any time. Layer hashes re-counted 13:33 (driver scratch `lh_*.iter37.txt`): 1088/1152 differing, first at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`); dump files unchanged (mtimes 12:52/12:54). Manual serve pid 21537 alive (up 05:12:39), `/v1/models` 200; `pending.json` still holds the deferred `restart-primary`; ~1 h 27 min to the ~15:00 UTC budget exit. Next iteration: expect the tree fingerprint to have moved (new edits) even if the session is still alive. |

## Resolved

| date (UTC) | blocker | resolution |
|---|---|---|

## Standing notes (awareness, no action needed)

- **Concurrent interactive session (2026-08-18 ~09:15):** an interactive `pi` TUI (pid 21662,
  up since 08:22) works in this repo alongside the unattended driver — it committed `18b4b674`
  and `855acadd` and is researching MTP acceptance (Exa helpers at `tools/autoninfer/exa_*.sh`).
  The driver's serve-op deferral already detects live pi sessions. If the interactive session and
  a driver iteration collide on GPU 1 or `handover.md`, the interactive one wins by default — the
  driver iteration should back off. Killed only by the user.
- **Instance restart (stop/start or reboot) is always safe**: the supervised primary serve and
  this loop both autostart, and the container filesystem (including the model artifact)
  survives. If a GPU-wedge blocker above asks for a restart, that is the *only* known recovery
  (userspace GPU reset is unavailable on this host).
- **Instance recycle/destroy is not safe**: it wipes the container filesystem. Reconstruction:
  re-clone `fikrikarim/autoninfer`, restore the git-ignored `models/qwen3_8_27b_nvfp4.ninfer`
  (21 GiB) from its source, run `bash tools/autoninfer/install_services.sh`, restore the models
  symlink (`ln -sfn /workspace/autoninfer/.pi/models.json /root/.pi/agent/models.json`).
- **Budget**: the loop runs ~30 h (wall clock, from `2026-08-18 ~09:00 UTC`) then exits cleanly.
  If it has stopped with no active blocker and you want it to continue:
  `supervisorctl start autoninfer-driver`.
- **Monitoring**: `docs/autoninfer/results.tsv` (one row per experiment), `git log` (one commit
  per iteration), `docs/autoninfer/handover.md` (what the next iteration will do),
  `/var/log/autoninfer-drive.log` and `/var/log/portal/ninfer-serve.log` (over SSH).
- **Design note — why one agent, not several**: the research GPU (one 5090) serializes every
  experiment anyway; the repository is single-writer; and the serve's `--max-concurrency 2`
  leaves at most one lane for a second agent. A second agent would contend for the harness's own
  model lane and the research GPU without adding throughput. The loop is single-agent by design.
  Interactive sessions and the loop share the serve safely (`--pending-timeout-ms 300000` lets
  the second request queue through a long interactive turn).
- **Quality policy** (user rule, 2026-08-18): the north star is maximum speed; a small *measured*
  quality degradation is acceptable for a large speedup (BF16→NVFP4-class tradeoffs are fine). It
  is quantified with `tools/autoninfer/quality_gate.sh` (greedy 8-prompt gate, per-prompt token
  hashes), not assumed: pure perf changes must be token-identical; numerics changes keep only at
  ≥5% M1 gain with a small diff; ≥3/8 prompts changed goes here for your ratification when the
  speedup is large. Full rules: Ground rules 7 in `docs/autoninfer/README.md`.