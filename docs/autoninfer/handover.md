# Autoninfer handover

**Last updated:** 2026-08-18 ~22:50 UTC by unattended driver iteration 19 (backoff #1 on the Op 3 dirt — yielded the tree, produced a result: backlog #5 closed with a measurement row).
**Result this iteration: MTP host-side round-overhead audit (backlog #5) — measurement row added, NO cheap token-identical k=3 M1 lever exists at the round/host boundary; the round is ~90% device-bound. Tree untouched (owned).**

## HEAD & state
- **HEAD:** `daf6608d` (DSpark Op 1 landing docs) + this iteration's docs commit. **Tree is DIRTY — owned by the live interactive session (see below).**
- **Canonical baseline M1 (k=3):** `113.01 ± 0.05 tok/s, 30.15% accept` (82ef8337). This iteration re-confirmed **113.92 tok/s / 30.2% accept** (tg128 r=1, GPU 1, 22:33 — same session as the audit op benches). Same-session A/B refs (still valid): k=3 113.93 / k=4-lm-head 140.36 / k=5 138.33.
- **Two open user decisions (BLOCKERS, neither ratified — k=3 stays canonical):** (1) 19:55 row: ratify MTP **k=4-lm-head** canonical (+23.2%, 140.36). (2) 17:45 row: ratify closing the MTP losslessness series.
- **Serve state (do not touch):** manual serve pid 21537 (k=3 flags) live on GPU 0, 200 on 8080; supervisor `ninfer-serve` FATAL (port collision with the manual serve — the driver self-heals off `/v1/models`, leave it); standby never started. `/tmp/autoninfer-ops/pending.json` **empty** (re-verified 22:44) — do not re-queue `restart-primary` unless a flag change is genuinely required.
- **GPU 1:** HEALTHY at 22:29 (triad 1467.1 GiB/s, FMA 111.28 TFLOP/s). All audit GPU work ran on GPU 1 via copied binaries in `/tmp/it19-bin/` (avoided exec-while-rebuild races with the owner).
- **bg.sh ledger:** empty at start, nothing registered this iteration (all audit work ran foreground, no detached jobs).

## The dirty tree — facts for the next iteration (decide fresh per the concurrent-session rule)
- **Owner: interactive pi session pid 21662** (alive at 22:44; ancestor chain: 21662 → 3785 bash → 3505 `herdr server` = daemon-held user tty). It is working the **DSpark Op 3** line (`dspark_block_attention` + `dspark_block_decode` = the drafter's dual-source GQA attention + 5-layer forward, plus new BF16 linear draft shapes).
- **WIP inventory (verified 22:29):** modified: `include/ninfer/ops/dspark_ctx_commit.h` (+1: kQHeadCount=40), `src/CMakeLists.txt` (+4), `src/ops/linear/bf16/{bf16_dispatch.cpp,bf16_gemm_mma.cu,bf16_gemv.cu,bf16_small_t.cu}` (+66: additive shapes 7168/5120, 5120/5120, 20480/5120, 5120/10240 — no existing-shape re-routing), `tests/CMakeLists.txt` (+2); new: `include/ninfer/ops/dspark_block_{attention,decode}.h` (113/124 lines), `src/ops/dspark_block_attention/` (293+26+97 lines), `src/ops/dspark_block_decode/` (130+24+175 lines), `tests/ops/test_dspark_block_{attention,decode}.cpp` (265/465 lines). `git diff | sha256sum` = `e78b00608a84123336896f3c1c5810b7b81e0101a4a7521352941da411074e1c`.
- **Timeline (mtimes):** files written 21:34 → **22:21:51** (last: `dspark_block_attention.cu`); owner built 22:21–22:24 (build/ = WIP state); debug probes in /tmp: `tensor_probe.cpp` 21:47, `probe2` 21:50, `dbg_attn.py` 22:10 (they were debugging attention numerics); quality gate `pre-dspark-op3` ran 21:34 → **`/tmp/quality_gate_pre-dspark-op3.jsonl` is a HEAD-equivalent pre-gate reference (8 per-prompt hashes)** — reuse it as the pre-gate instead of re-running, until the next numerics change lands.
- **Activity at check time:** no compiler processes and no 21662 children at 22:29/22:44 — but the last source write was only ~20 min before the 22:44 check, so **NOT converged by the 30-min rule at check time**. (Iter 19 counted as backoff #1 on this dirt *with a result* — it ran the independent backlog #5 audit instead of idling.)

## What was done (one experiment: backlog #5 round-overhead audit — measurement only, no code change)
Op-level benches (M3) on GPU 1 with `/tmp/it19-bin/` binaries (WIP-tree build, but byte-identical for the audited ops — the WIP diff is inert for argmax/mtp_pack/sampling_select/Q4-linear, verified by diff inspection):
1. **Round-boundary ops are trivial:** argmax full-vocab T=4 8.20 µs / T=1 5.96 µs, shortlist T=1 6.35 µs; mtp_pack T=4 3.26 µs; mtp_split T=4 0.91 µs; sampling_select G3 K=3 greedy 8.80 µs → **~21 µs/round device + D2H/sync ≈ 0.25% of the 16.8 ms round** (round time = 384 tok / 113.93 tok/s / 201 rounds, cross-checked with the 67-round tg128 r=1 run).
2. **Draft head (the big non-verify cost) is at the roofline:** the optimized proposal head is **Q4 `131072×5120`** (not FP8 — the FP8 head is the full 248K `lm` head from the earlier A/B), T=1 GEMV **235.5 µs at 90.5% of the 1674.5 GB/s sustained-read roofline** (`ninfer_linear_bench --suite all`, 27b.draft_head row). No bandwidth headroom → no token-identical GEMV win. Draft path at k=3 ≈ 3 × (235.5 µs head + proposal layer) ≈ 1.0–1.4 ms/round ≈ 6–8%.
3. **Host gap:** nsys 2026.2.1 and ncu (nsight-compute 2026.2.1) both fail to capture ANY kernel in this container (single `cuModuleGetLoadingMode` call captured, then nothing — injection is broken with the host-injected libcuda in the unprivileged container). Host overhead is therefore bounded only: by (1)+(2), round = T1-verify ~14.0 ms (published MTP0 71.2 tok/s; T=1 routes unchanged by the bit-clone campaign) + MTP@k3 +2.8 ms (draft + verify columns + boundary) → host gap ≪ 5% keep threshold. **No token-identical host fix can clear the bar; #5 closed as a discard-measurement.**
4. **Round decomposition (the attribution that matters):** ~90% device-bound at k=3. Levers in rank order: **k=4 (BLOCKERS 19:55, +23.2% measured)** → **DSpark lane (Op 3 in progress)** → prefill/M2 (backlog #2, separate metric) → draft-path step reduction (schedule change = numerics, near-tie gate risk, subsumed by the k=4 decision).

## Next iteration (single next step)
**DSpark Op 3 — check state, then either take over or run the fallback audit.**
1. Re-check: `ps -o pid,etime,cputime -p 21662`; `git diff | sha256sum` (expect `e78b0060…`); `stat -c '%y' src/ops/dspark_block_attention/dspark_block_attention.cu` (22:21:51 at last check) + `ls -lat build/ | head -5` + `ls -lat /tmp | head -12` for new owner writes/probes.
2. **If the WIP has been quiet 30+ min (no writes to its files, no build/ churn) → take over** (rule a): build `cmake --build build -j` (main tree), run `ninfer_dspark_block_attention_test` + `ninfer_dspark_block_decode_test` + the dspark ctx_commit/tap suites + the bf16 linear suites (GPU 1), verify the contract incl. the per-column T=1-vs-T=7 bit-parity the lane doc requires, strip any TEMP instrumentation, commit + push (one commit), then M1 (menu) + gate `tools/autoninfer/quality_gate.sh post-dspark-op3` vs `/tmp/quality_gate_pre-dspark-op3.jsonl` (engine-unwired → must be **8/8 token-identical**); results row; handover moves to Op 4 / backend wiring per `docs/maintainer/qwen3.8-27b-dspark-lane.md`.
3. **If still actively owned (new writes/build churn) → fallback experiment so the iteration has a result: M2 prefill audit (backlog #2)** — needs a clean build (build/ is WIP): `cp -a /workspace/autoninfer /tmp/wt-it20` (mtimes preserved), `cd /tmp/wt-it20 && git checkout -- . && rm -rf src/ops/dspark_block_attention src/ops/dspark_block_decode include/ninfer/ops/dspark_block_*.h tests/ops/test_dspark_block_*.cpp` (verify `git status` clean at daf6608d), `cmake --build build -j --target ninfer_bench` (incremental — only reverted files rebuild; **check `df -h` first: ~26 GiB free, cp -a ≈ 19 GiB**), then M2 menu `CUDA_VISIBLE_DEVICES=1 ./build/bench/ninfer_bench --weights models/qwen3_8_27b_nvfp4.ninfer -p 2048 -r 3 --warmup 1 --kv-dtype int8` + the FP8 prefill crossover benches (`ninfer_fp8_linear_add_bench --k 6144 --policy a8 --t-sweep 1024,1536,2048` / `--k 17408 ...`, `ninfer_fp8_linear_swiglu_bench --policy a8 --t-sweep 1024`), compare to the published 8,340 tok/s prefill lag (1.34× vs qwen3.6 nvfp4). A prefill GEMM crossover is a numerics change → gate pre/post (pre = `/tmp/quality_gate_pre-dspark-op3.jsonl` is decode-path only — run a fresh pre-gate on the clean binary first), keep bar: ≥5% M2 gain (M1 unaffected by prefill-only changes → also verify M1 unchanged).

## Do not repeat / do not touch
- Re-running the backlog #5 audit benches (argmax/mtp_pack/sampling_select matrix, draft-head linear points) — numbers are in the results row + this handover.
- Retrying nsys/ncu in-container (both fail: injection broken with the host-injected libcuda; verified 22:36–22:40 on a trivial app).
- The Op 3 WIP files while the owner session is active; editing the main tree at all while it is (its builds pick up stray edits).
- The manual serve (pid 21537) and GPU 0 in any way; no standby serve; do not re-queue `restart-primary` (pending.json empty).
- Never commit `.env`, `models/`, `out/`. `git stash @{0}` (iter-14 rx-dump tap) and `@{1}` (layer/logit tap) — don't drop either.
- `/tmp/wt-it13` (worktree @ 18198e78, committed-source binaries for the k=4 A/B) — keep until the k=4 decision is settled; `/tmp/wt` — removable.
- **Disk:** ~26 GiB free, main `build/` 19 GiB — a second *full* build dir does not fit; use the cp -a copy + incremental ninja pattern (step 3 above) and `rm -rf /tmp/wt-itNN` when done.
- **Engine quirk (incidental, not chased):** `ninfer_bench --mtp-draft-tokens 0 --max-ctx 16384` fails with "CUDA Graph preparation consumed 62914560 bytes, exceeding the planned allowance of 12582912 bytes" — the k=0 graph allowance planning at 16K ctx is wrong (planned 12 MiB, needs 60 MiB). MTP0 decode is still bracketable via the published 71.2 tok/s.
- **Test stream discipline:** `GuardedDeviceBuffer::fill`/`DeviceBuffer::copy_from_host` run on the LEGACY default stream; Op calls run on a non-blocking stream — stage host prep, then `cuda_synchronize()` before the first op call (lesson from the Op 1 iteration).
- GPU 1: `bash tools/gpu_health.sh 1` before any benchmark session.