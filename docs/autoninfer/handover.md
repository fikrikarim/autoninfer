# Autoninfer handover

**Last updated:** 2026-08-18 23:52 UTC by unattended driver iteration 21 — **RESULT (c): recorded decision — DSpark Op 3 WIP is mid active debug by the interactive user; loop STOPPED via `/tmp/autoninfer-stop`; BLOCKERS row 23:49 filed. No code change, no GPU work.**

## HEAD & state
- **HEAD:** `f70e1a1c` (backlog #2 prefill audit, driver iter 20) + this iteration's docs commit on top.
- **Tree is DIRTY — DSpark Op 3 WIP, actively owned and mid-debug** (facts below). Do not build/measure/commit on it while writes are fresh.
- **Canonical baseline M1 (k=3):** `113.01 ± 0.05 tok/s, 30.15% accept` (82ef8337). Unchanged (no code change this iteration). k=3 stays canonical until the user ratifies k=4 (BLOCKERS 19:55, +23.2%).
- **Serve state (do not touch):** manual serve pid 21537 (k=3 flags) live on GPU 0, 200 on 8080; supervisor `ninfer-serve` FATAL (port collision — leave it); standby never started; `/tmp/autoninfer-ops/pending.json` empty.
- **GPU 1:** last HEALTHY stamp 22:38 (triad 1467.7 GiB/s, FMA 111.27 TFLOP/s). No new stamp this iteration (no GPU work — blocked). Re-verify with `bash tools/gpu_health.sh 1` before any benchmark session.
- **bg.sh ledger:** empty; nothing registered (no detached work this iteration).
- **Pre-gate reference (reuse, do not re-run):** `/tmp/quality_gate_pre-dspark-op3.jsonl` (21:34, HEAD-equivalent, 8 per-prompt hashes) — valid until the next numerics change lands.

## The dirty tree — facts (23:44-23:50 checks; supersedes iter 20's)
- **The Op 3 WIP is being actively debugged by the interactive user, writes up to 23:43:01 (43 s before this iteration started).** Untracked WIP file mtimes: `dspark_block_attention.cu` **23:43:01**, `dspark_block_attention.cpp` 23:17:53, `dspark_launch.h` (attention) 23:17:39, `dspark_block_decode.cpp` 23:17:13, `test_dspark_block_decode.cpp` 23:05:37, `dspark_qk_rope.cu` 21:40:53, headers 21:34/21:40. Tracked WIP files (bf16_*, `dspark_ctx_commit.h`, CMakeLists) unchanged since 21:37-21:40 (tracked diff fingerprint `e78b0060…` — but that fingerprint is BLIND to the untracked op files; iter 20's 22:51:51 convergence mark was computed from it and is VOID).
- **Debug evidence in /tmp (owner scratch, do not delete):** `dbg_attn2` (+.cpp) 23:00/23:08 — "constant inputs → uniform softmax → output must be exactly the V constant" multi-split repro; `dbg_fit` 23:16 — exact-fit arena sub-allocation vs `dspark_block_decode_workspace_capacity_bytes`; `dbg_env` 23:34/23:40 — "reproduce the attention test's exact data (same RNG sequence) for layer=2 start=127 and diff the tight vs wide envelope outputs elementwise". Interpretation: the attention op test shows an **envelope-dependent output discrepancy** under active hunt.
- **The op code carries TEMP scaffolding:** `dspark_block_attention.cu` passes `getenv("DSPARK_DBG") != nullptr` as a debug arg to the combine-kernel call — strip before any commit (grep for `DSPARK_DBG`, not just `NINFER_DEBUG` — this iteration's first grep missed it).
- **Liveness:** interactive pi 21662 (pts/6) alive, etime 15:24, CPU 01:17:40 **flat since 22:42** (iter 19/20 checks) — the writes are NOT from a pi agent; they come from the user's **herdr tty directly** (`herdr` pid 3504 on pts/1 holds pts/5/6/7 bash sessions; `nvtop` running on pts/7). No ninja/cicc/nvcc running at the 23:44 check, but the write cadence (23:00→23:43, every few minutes) says mid-session.
- WIP content (from iter 20 + diff inspection): `dspark_block_attention` (split + combine GQA over the persistent draft-KV arena, T=7 draft blocks) + `dspark_block_decode` (draft forward glue incl. `dspark_qk_rope`) + 4 new BF16 draft linear shapes (7168/5120, 5120/5120, 20480/5120, 5120/10240) in bf16 dispatch/gemm_mma/gemv/small_t. Proven additive by iter 20's diff inspection (no existing-shape re-routing) — but that was at 22:42; the 23:xx edits may have changed that, re-verify before trusting M1-neutrality.

## What this iteration did
1. `bg.sh check` (empty ledger), liveness/fingerprint/mtime sweep.
2. Found the 22:51:51 convergence mark **void**: untracked Op 3 files were written 23:05-23:43:01 with active numerical debugging in /tmp (see above). The tracked-only fingerprint had masked this.
3. Decision per the concurrent-session rule (c) — takeover genuinely unsafe (owner mid-debug on unverified op code + live TEMP scaffolding; stashing a live session's WIP is forbidden): filed the BLOCKERS 23:49 row, wrote `/tmp/autoninfer-stop` (reason in the file), committed + pushed docs. **No GPU work, no build, no edit of WIP files.**

## Next iteration (decide FRESH from current facts — this handover records state, not instructions)
0. `bash tools/autoninfer/bg.sh check` (expect empty; if the user re-armed the driver, `/tmp/autoninfer-stop` should be gone — if it still exists, the user has not cleared it: check the BLOCKERS 23:49 row for a verdict, act on it, do not loop again on the same dirt).
1. **Convergence check (use untracked-file mtimes, NOT the tracked fingerprint):** `stat -c '%n %y' src/ops/dspark_block_attention/* src/ops/dspark_block_decode/* include/ninfer/ops/dspark_block_*.h tests/ops/test_dspark_block_*.cpp`, plus `ls -lat /tmp | head -12` (owner scratch cadence), `pgrep -x ninja cicc nvcc` (build churn), `ps -o pid,etime,cputime -p 21662` (owner liveness; flat CPU + gone = owner left).
   - **Owner gone (pid 21662 dead or ttys gone) → rule (b):** `git stash push -m "DSpark Op 3 WIP (interactive, abandoned 23:43)"` (it becomes stash `@{0}` — record the new index here; the rx-dump/layer-logit taps shift to `@{1}`/`@{2}`), then proceed to the **M2 graph-allowance fix** (below) on the clean HEAD.
   - **Owner quiet 30+ min (latest mtime below now-30m, no build churn) → rule (a) takeover:** `cmake --build build -j`; on GPU 1 (health check first): `./build/tests/ninfer_dspark_block_attention_test`, `ninfer_dspark_block_decode_test`, the dspark ctx_commit/tap suites, the bf16 linear suites; verify the per-column T=1-vs-T=7 bit-parity the lane doc (`docs/maintainer/qwen3.8-27b-dspark-lane.md` §4, Op 3) requires — the envelope-dependent discrepancy the owner was hunting must be resolved for the tests to pass; **strip the `DSPARK_DBG` scaffolding** (and anything else TEMP: grep `DSPARK_DBG`, `NINFER_DEBUG`, `dbg_`); rebuild + rerun; commit + push as one feat commit; `tools/autoninfer/quality_gate.sh post-dspark-op3` vs `/tmp/quality_gate_pre-dspark-op3.jsonl` (engine-unwired → must be 8/8 token-identical); M1 menu (expect ≈ 113.0 baseline; if the 23:xx edits broke iter 20's additivity proof, a diff here means M1 numerics moved — investigate); results row; then Op 4 (`dspark_markov_logits`) / backend wiring per the lane doc.
   - **Still actively written (fresh mtimes, or owner alive and compiling) → rule (c) again:** update the BLOCKERS 23:49 row state with the new evidence (do NOT file a duplicate row), re-write `/tmp/autoninfer-stop`, docs commit, end.
2. **M2 graph-allowance fix (ready once the tree is clean — token-identical engine fix):** `ninfer_bench` / any k=0 or prefill-only Engine config dies at graph prep: "CUDA Graph preparation consumed 62914560 bytes, exceeding the planned allowance of 12582912 bytes" (`src/targets/qwen3_6/impl/runtime/program_impl.h:1390`) — the graph allowance plan under-reserves (12 MiB planned vs 60 MiB consumed), context-size independent (fails at max-ctx 2048 AND 16384); M1 k=3 works, so only non-MTP graph-family configs hit it. Fix, build, quality gate (must be 8/8 identical vs `/tmp/quality_gate_pre-dspark-op3.jsonl` unless a numerics change landed meanwhile), M1 (must match 113.0 baseline — token-identical), then re-run the M2 menu command **without** `--no-cuda-graph` to close the prefill-audit loop.

## Do not repeat / do not touch
- The prefill audit benches (M2 points, A8/A16 crossovers @ T=1024, GDN chunked breakdown) — numbers in the 902ff970/f70e1a1c results rows + iter 20 handover; CSVs in `/tmp/it20-*.csv`, binaries in `/tmp/it20-bin/` (keep until the next numerics change).
- nsys/ncu in-container (injection broken with host-injected libcuda — verified iter 19).
- The manual serve (pid 21537) and GPU 0 in any way; no standby serve; no `restart-primary` re-queue (pending.json empty).
- Never commit `.env`, `models/`, `out/`. Stashes: `@{0}` = rx-dump tap, `@{1}` = layer/logit tap — do not drop either (if the Op 3 WIP gets stashed, it becomes `@{0}` and these shift — record the new indices).
- `/tmp/wt-it13` (worktree @ 18198e78, k=4 A/B binaries) — keep until the k=4 decision is settled; the owner's `dbg_*` files in /tmp — do not delete (live debug session).
- **Disk:** 26 GiB free; models 23 G + build 19 G — no second full build dir; use /tmp binary copies + incremental builds.
- **Test stream discipline:** host-side staging runs on the legacy default stream — `cuda_synchronize()` before the first op call in new test code.
- GPU 1: `bash tools/gpu_health.sh 1` before any benchmark session (no fresh stamp since 22:38).
- Open user decisions (BLOCKERS): 17:45 (close MTP losslessness series), 19:55 (k=4 canonical, +23.2%), 22:45 (BF16 checkpoint + disk + NVFP4 re-quant ratify), **23:49 (Op 3 WIP yield / loop stop)** — none ratified; k=3 stays canonical until then.
- Convergence judgment must use **untracked-WIP-file mtimes**, not the tracked `git diff` fingerprint (this iteration's lesson).