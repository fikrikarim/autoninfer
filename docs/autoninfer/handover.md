# Autoninfer handover

**Last updated:** 2026-08-18 13:35 UTC by unattended driver session (iteration 37) — **backed off (30th consecutive), no experiment this iteration** (interactive-session collision rule). State below.

## State
- **HEAD:** `7fbee97b` (iteration 36's backoff docs commit); this iteration adds a backoff docs commit on top.
- **Baseline M1 (keep/discard reference):** `130.13 ± 0.14 tok/s, 38.4% accept` at k=3 (tg128, `--lm-head-draft`, INT8 KV, menu command), set at e84ddaef.
- **Working tree is DIRTY — owned by the live interactive pi session (pid 21662), which is now ACTIVELY WORKING, not idle.** At this iteration's check (13:32–13:33):
  - **pid 21662** (pts/6, ppid 3785 `herdr server`, up 05:09+, CPU 29:59 → 31:16 → 31:24 — still climbing ~75–85 s/min, stat Rl+).
  - **At 13:32 the session launched a rebuild of the WIP tree**: its child **pid 245332** runs `git diff tests/CMakeLists.txt && cmake --build build -j 2>&1 | tail -4` (ninja → nvcc → cicc/ptxas active; fresh `tmpxft_*.cpp4.ii` nvcc temp files in /tmp at 13:32 are this build's artifacts, **not** new WIP writes). They are presumably re-running `test_fp8_linear_t_parity` after the build.
  - **WIP diff fingerprint (13:32):** `git diff | sha256sum` = `d49b4f84…bb705` — still byte-identical to iterations 11–37; **no repo edit since 12:54:09**, but with the session actively rebuilding a new edit may land at any time. **Expect the fingerprint to have moved by the next iteration** even if the session is still alive.
  - Note: their build rewrites the shared `build/` directory; when they finish, `build/` holds the WIP state.

## Fresh /tmp evidence (read at 13:32–13:33, supersedes iteration 36's)
- Last interactive **repo** write: still 12:54:09. The only non-driver /tmp files newer than it besides the driver's `lh_*.iter*` scratch are the `tmpxft_*` nvcc temp files (13:32, build artifacts of pid 245332's build) and the 13:07:07 `lh_{old,new}_keys.txt` (driver iter-16 scratch, already documented).
- Layer-hash dump state, re-counted this iteration (scratch `lh_*.iter37.txt`, paste-join in file order): both dumps 1152 tuples; **1088 differing, 64 matching; first differing tuple: line 9 = run=150 mix layer=4** (old h=`478a57cad0f3a017` vs new h=`4a44cf19f097a851`). Unchanged since iteration 15. Dump files themselves unchanged (mtimes 12:52/12:54). The WIP FP8 fix was **not verified clean** at completion of their last AIME run.

## Budget note
Driver budget is 30 h wall clock from ~09:00 UTC (2026-08-18) → clean exit around ~15:00 UTC (~1 h 27 min remaining at 13:35). If the interactive session stays alive, remaining iterations continue to back off in docs-only mode (by design; the driver stops cleanly on budget, and `supervisorctl start autoninfer-driver` re-arms it).

## Serve state (do not touch)
- The live harness serve is the **manual** process (**pid 21537, up 05:12:39 at 13:33**) on GPU 0: `ninfer-serve models/qwen3_8_27b_nvfp4.ninfer --host 127.0.0.1 --port 8080 --max-context 262144 --kv-capacity 262144 --kv-dtype int8 --max-concurrency 2 --spec mtp --draft-tokens 3 --lm-head-draft` (lacks `--pending-timeout-ms 300000`). Supervisor `ninfer-serve` is STOPPED (since 08:30); `ninfer-serve-standby` STOPPED (since 08:44). `/v1/models` → 200 at 13:33.
- `/tmp/autoninfer-ops/pending.json` still holds `{"action":"restart-primary"}` (queued 08:45, deferred while interactive sessions are alive; re-verified 13:33). Expect the **supervisor** serve (wrapper flags incl. `--pending-timeout-ms 300000`) to come up once no interactive session is alive. Verify at start: `supervisorctl status ninfer-serve` + `curl -s http://127.0.0.1:8080/v1/models`. Do NOT re-queue it.
- GPU 1 idle (no GPU work ran this iteration — backoff — so no fresh `tools/gpu_health.sh 1` stamp).

## The interactive session's WIP (unchanged in content since 12:54:09; they were rebuilding it at 13:32)
- `src/ops/linear/fp8/fp8_config.h`, `fp8_dispatch.cpp`, `src/ops/linear_add/fp8/fp8_linear_add_small_t.cu`, `src/ops/linear_swiglu/fp8/fp8_linear_swiglu_plan.cpp` — T=2..4 small-t schedules become the T=1 GEMV bit-clone (vpl=8, four interleaved accumulator chains, same pair-to-chain mapping); A8 parents (FP8 MLP gate/up) stay A8 at all T.
- `src/targets/qwen3_6/impl/runtime/text_context_impl.h` — TEMP `NINFER_DEBUG_LAYERHASH` per-layer hidden-hash diagnostic, marked "not for commit"; must be stripped before commit.
- `tests/ops/test_fp8_linear_t_parity.cpp` (new, the real test: T=1..4 per-column bit parity), `tests/ops/test_tmp_binary_diff.cpp` (new, temp op-dump harness — must not be committed), `tests/CMakeLists.txt`, `tests/ops/linear/test_fp8_a16.cpp`, `tests/ops/linear/test_fp8_a8.cpp` (schedule expectations).

## Next iteration, in order
1. **Check whether the interactive session is alive:** `ps -o pid,etime,cputime,stat -p 21662` (track only 21662; plus `ps aux | grep -w "pi"` — exclude the driver's own session via the parent chain: its grandparent is `drive.sh` under `supervisord`), then `git status --short`, `git log --oneline -3`, and `git diff | sha256sum` (**expect the fingerprint to have MOVED off `d49b4f84…bb705`** — the session was actively rebuilding at 13:32 and may have made new edits since). Also check for a fresh build in flight: `ps aux | grep -E 'nvcc|ninja' | grep -v grep` (a live build = actively working).
   - **Interactive pi alive (especially with a dirty tree or an in-flight build) → back off exactly as iterations 8–37 did** (refresh this handover from fresh `/tmp` evidence — last-write scan `ls -lat /tmp | head -15` plus `find /tmp -maxdepth 1 -type f -newermt '2026-08-18 12:54:10' ! -name 'lh_*.iter*' ! -name 'lh_pasted.iter*' -printf '%T@ %p\n' | sort -n | tail` — interpret `tmpxft_*` nvcc temp files as *build* artifacts and check their parent process before calling them interactive writes, `stat` the WIP files, re-count layer-hash diffs: `grep -oE 'LH run=[0-9]+ (mix|mlp) layer=[0-9]+ h=[0-9a-f]+' /tmp/lh_old.err` / same for `lh_new.err`, then `paste` the two extracts and `awk '$2!=$7 || $3!=$8 || $4!=$9 || $5!=$10 {d++} END{print d}'` per row — 1088/64 with first diff at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`) is the current expectation if the dumps are unchanged — commit ONLY the handover + BLOCKERS state if it moved, push, end). Do not touch the tree. Driver scratch in /tmp (`lh_*.iterNN.txt`, `lh_pasted.iterNN.txt`, `tmpxft_*` from builds started by a driver session) is not interactive work; check mtimes and process parents. If 21662's CPU time keeps climbing, that is activity (still back off while the tree is dirty — same rule).
   - **Committed → verify its commit:** contains the FP8 fix + `test_fp8_linear_t_parity`; does NOT contain `NINFER_DEBUG_LAYERHASH` or `test_tmp_binary_diff` (grep the new tree). If they leaked in, record a BLOCKERS row; do not silently rewrite another session's commit.
   - **Sessions gone, tree still dirty (abandoned WIP):** the driver may now take over — but treat the uncommitted diff as someone else's half-finished experiment: build it, run `test_fp8_linear_t_parity`, re-run the AIME k=0/k=1 diff (the `m_*.err` pattern / `matrix.sh` in /tmp), and only commit it if the evidence shows the fix is correct and the TEMP instrumentation is stripped; otherwise `git stash` it with a descriptive message and proceed to backlog #2, noting the stash in the handover.
2. **Clean rebuild** `cmake --build build -j` once the tree is clean/owned (build/ currently holds the WIP state, possibly mid-build).
3. **Measure at the new HEAD** (FP8 committed-column clone in): M1 menu command (k=3) — expect the accept rate to shift (compare to the attention fix's +8.2pp). New baseline row. Then the backlog **k=2 vs k=3 re-decision** (pre-fix A/B numbers are stale): same menu command with `--mtp-draft-tokens 2` vs `3`.
4. **Quality gate:** `tools/autoninfer/quality_gate.sh pre-<exp>` / `post-<exp>` around any further change; after the FP8 fix expect the k=0-vs-k=3 gate diff to shrink below the current 4/8 and the AIME k=1 idx-119 flip (280 vs 343) to disappear — verify by re-running the k=0 and k=1 AIME commands and diffing the token-id lines.
5. Backlog after that: prefill FP8 crossovers (backlog #2), host-side round overhead (backlog #5). **Web-research status (iter 17): all five backlog hypotheses already have concrete seeded entries in `docs/autoninfer/inspiration.md`** (H2's vLLM NVFP4 tile-sweep template is a ready-made experiment for backlog #2) — no new research needed before the next experiment; search only if a measurement is surprising.

## Do not repeat / do not touch
- The k=1 flip attribution — owned by the interactive session (FP8 linear small-t T=2..4 association + A16 island). Their evidence is in `/tmp` as listed above; their WIP was **not verified clean at completion** (13:33: 1088/1152 layer-hash tuples still differ, first at line 9 = run=150 mix layer=4; no repo edit since 12:54:09; **they were actively rebuilding at 13:32** — the activity pattern changed from "CPU flat/idle" (iters 17–34) to "CPU climbing + building" (iters 35–37)).
- Building/measuring/committing on the dirty tree while the interactive pi session (pid 21662) is alive.
- The manual serve (pid 21537) and GPU 0 in any way (bind/kill/restart/reconfigure).
- Starting the standby serve. Re-queueing `restart-primary` (already pending). Committing `.env` (git-ignored, EXA key).
- GPU 1: `bash tools/gpu_health.sh 1` before any benchmark. No GPU work ran this iteration (backoff #30).
- Speculative web-research surveys while blocked: the backlog is seeded; research only feeds a live decision.