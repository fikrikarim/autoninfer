# Autoninfer handover

**Last updated:** 2026-08-18 by an unattended driver session (iteration 6; experiment: attention committed-column bit-clone — implemented by the timed-out iteration 5, verified, kept, committed, pushed).

## State
- **HEAD:** `e84ddaef` (fix(ops): make the GQA small-T verify committed column a bit-clone of the T=1 decode route), pushed.
- **Baseline M1 (keep/discard reference):** `130.13 ± 0.14 tok/s, 38.4% accept` at k=3 (tg128, `--lm-head-draft`, INT8 KV, menu command) — set this iteration (was 114.35 ± 0.04 / 30.2%).
- **MTP losslessness status:** the committed (first) column is now a **bit-clone of the T=1 route** — op-level parity bit-exact (I8+BF16, T=1..6, 10 committed windows incl. tile/split-tier boundaries and the M1 ceiling). k=0 AIME greedy stream bit-unchanged (`434280f5`, canonical). Residual: k=1 vs k=0 a **single near-tie token flip** (AIME idx 119: 280 vs 343) that reconverges 7 tokens later — localized to the verify-round draft column (attribution below). Post-fix 8-prompt gate (256 tok, k=0 vs k=3): **4/8 exact** (pre-fix 3/8); the 4 differing prompts (code-neighbor-sum, translate-fr, math-modexp, haiku) are same-length, clearly-correct outputs — BLOCKERS row added for user ratification of this residual.

## This iteration (finished iteration 5's unfinished attention fix)
The tree arrived dirty: driver iteration 5 (10:43–11:42, hit the 1 h timeout) had implemented the handover's attention fix but never verified, committed, or measured. I reviewed the whole design, then built and verified. Design invariants confirmed by code reading:

- **Warp counts are a numerics no-op** in both small-T partial kernels: I8 producers are one warp per 16-row tile over the FULL split key range (per-row m/l/P), consumers partition only the 256-wide head-dim; BF16 warps own 16-row groups over the full range. What fixes per-column arithmetic is the **KeyBlock** (online-softmax rescale sequence) and the **split partition**.
- **New committed-column partition** (`gqa_small_t_split_range` in `gqa_attention_decode.cuh`): active count + per-split units derived from the committed window only (the exact T=1 partition); the draft columns' keys `[W0, W_T)` fold into the split owning `W0-1`, covering `[0, W_T)` exactly once. For column 0 the tail is causally masked — bit-exact no-op (scores -inf → p = 0 exact, alpha = 1, 0·V = 0, no -inf/-inf NaN since column 0 always has an unmasked key in every split it spans).
- **INT8 T=6 keeps the 32-key tile at every window** (the 64-key 2K..8K tier, the last T-dependent KeyBlock, removed). Launch capacity now uses the width-independent T=1 split policy; `gqa_small_t_split_upper_bound` bounds it pointwise and is non-decreasing, so the grid never clamps the committed count.

Verification (all GPU 1): `ninfer_gqa_attention_parity_test` (new, committed — production profiles: T=1 exact unmasked envelope, T>1 verify envelope + device valid-columns tensor; committed windows 18/64/96/141/201/1004/1029/4096/8198/16384; I8 + BF16) → **0 bit diffs** across T=1..6. `ninfer_gqa_attention_test` PASS (I8 gross criterion recalibrated 1.1e-3 → 1.2e-3 to the new worst-case profile — T=6 short-window; T=1 window-18 case added). ctest 89/90 (the one failure = pre-existing missing-checkpoint `ninfer_qwen3_6_frontend_test`). M1 k=3: **130.13 ± 0.14 tok/s / 38.4% accept = +13.8% / +8.2pp** vs 114.35/30.2% — the width-dependent arithmetic had been suppressing acceptance (verify target argmax + next-round draft hidden perturbed). E2E AIME: k=0 `434280f5` (bit-unchanged); k=1 single flip at idx 119, reconverges.

## Next iteration: attribute the k=1 residual flip (col0 vs col1)
Contract recap: emitted stream = correction (col0 argmax) + accepted drafts/bonus (verify column argmaxes **emitted as-is**, per `speculative_accept_greedy_drafts` and model-doc 8). The committed column is now bit-verified end-to-end at the op level (attention this iteration; GDN at `5cbba58c`; linear families T≤4 association-clone). So the flip at AIME idx 119 is either:
- **(a) col0 correction** → a residual committed-column defect in a not-yet-bit-verified component (candidates: lm_head `ops::linear` T=2 verify-width association at the actual hidden scale, verify-input preparation, GDN fold/replay, I8 KV quantize of new rows); or
- **(b) col1 bonus/accepted-draft argmax** → an intrinsic finite-precision near-tie: the draft column's logits are a different (correct) finite-precision computation by design — one fused CTA partition cannot give every column the T=1 partition at its own window (definitely not across split-tier boundaries 4096/8198/16390).

First steps (≈1 iteration):
1. Instrument: the runtime has a `Tap`/`NullTap` template hook (`src/targets/qwen3_6/impl/runtime/text_context.h`, threaded through `run_layers`/`target_verify_batch`) or a temporary stderr print at the host-side MTP round transaction where the accept stats are aggregated (536 rounds / 486 accepted came from there). Log per round: verify ids (col0 anchor, draft), per-column target argmax, accepted count.
2. Re-run: `CUDA_VISIBLE_DEVICES=1 ./build/apps/ninfer models/qwen3_8_27b_nvfp4.ninfer --messages examples/cli/messages/long_decode_aime26_01.json --max-context 16384 --kv-dtype int8 --greedy --seed 42 --max-new 1024 --spec mtp --draft-tokens 1 --lm-head-draft --print-token-ids` (token ids on stderr, line `tokens  generated ids ...`; k=0 reference stream at /tmp/tok_k0.err if it survived, else regenerate the same command minus `--spec...`).
3. Find the round that emitted stream idx 119 (≈ round 62 at 1.91 tok/round) and check whether 343 was the col0 argmax (→ case a: hunt the unverified components) or the col1/bonus argmax (→ case b: document as intrinsic near-tie residual; per the quality policy it is a small measured diff buying the MTP speedup — record, no blocker; optionally quantify on the 8-prompt gate).
4. Do NOT repeat: the op-level attention parity (done, committed); the warp-partition analysis (settled: no-op). Do NOT assume case (b) without step 3 — the flip reconverging 7 tokens later is consistent with both.

## Backlog / notes
- **k=2 vs k=3 draft-window re-decision** (old A/B invalidated, now valid): committed-column losslessness holds; last numbers k=2 116.33 vs k=3 114.35 (+1.6%) — re-measure both with the menu command at HEAD (M1 at k=2: same command with `--mtp-draft-tokens 2`).
- Quality-gate baseline refresh (interactive-session-owned): post-fix gate state = the canonical k=0 stream for the committed column; the AIME k=1 near-tie flip is the known small residual.
- If case (b) confirmed: candidate follow-up = per-column T=1 partition for draft columns only when no tier boundary lies inside the round (most windows — partitions identical, near-zero cost); measure flip elimination before touching it.
- Don't commit `.env`. GPU 0 reserved (live serve); GPU 1 for all tests/benchmarks (`CUDA_VISIBLE_DEVICES=1`, `bash tools/gpu_health.sh 1` first). Harnesses in /tmp (recreate if wiped): `mtp_lossless.sh K`, `k0_k3_quality.sh`, streams `tok_k{0,1}.{out,err}`.