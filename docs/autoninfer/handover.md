# Autoninfer handover

**Last updated:** 2026-08-18 13:38 UTC by unattended driver session (iteration 39) — **backed off (32nd consecutive), no experiment this iteration** (interactive-session collision rule). State below.

## State
- **HEAD:** `27e4da3f` (iteration 38's backoff docs commit); this iteration adds one more backoff docs commit on top.
- **Baseline M1 (keep/discard reference):** `130.13 ± 0.14 tok/s, 38.4% accept` at k=3 (tg128, `--lm-head-draft`, INT8 KV, menu command), set at e84ddaef.
- **Working tree is DIRTY — owned by the live interactive pi session (pid 21662), which is ACTIVELY CONVERGING THE WIP:**
  - **pid 21662** (pts/6, ppid 3785 `herdr server`, up 05:14 at 13:37; CPU 33:34→33:46 — still climbing ~80 s/min; stat Rl+).
  - **13:36:44 new edit (the first since 12:54:09):** the session edited `src/targets/qwen3_6/impl/runtime/text_context_impl.h` and **removed the TEMP `NINFER_DEBUG_LAYERHASH` instrumentation** — the file is now unmodified vs HEAD and `grep -rn NINFER_DEBUG_LAYERHASH src/ tests/` finds nothing. This was one of the two "must be stripped before commit" artifacts.
  - **Also deleted `tests/ops/test_tmp_binary_diff.cpp`** (the second "must not be committed" artifact — the temp op-dump harness). Both temp artifacts are now gone from the working tree.
  - **13:36 new rebuild in flight:** `cmake --build build -j` (ninja 248953 ← bash 248951 ← 248949, started 13:36; cicc temps `cc*` in /tmp at 13:36:52). At 13:37 the build was still running.
  - **WIP fingerprint moved:** `git diff | sha256sum` = `7dade13f…fe151` at 13:37:01 (was `d49b4f84…bb705`). Current WIP = 7 modified files (`fp8_config.h`, `fp8_dispatch.cpp`, `fp8_linear_add_small_t.cu`, `fp8_linear_swiglu_plan.cpp`, `tests/CMakeLists.txt`, `test_fp8_a16.cpp`, `test_fp8_a8.cpp`) + 1 new untracked (`tests/ops/test_fp8_linear_t_parity.cpp`, the real bit-parity test). **The WIP is now in its cleanest form yet — it looks like it is about to be committed.**

## Fresh /tmp evidence (read at 13:36–13:38, supersedes iteration 38's)
- Last interactive **repo** write: **13:36:44** (text_context_impl.h edit + test_tmp_binary_diff.cpp deletion). The only non-driver /tmp files newer than the previous cutoff are the 13:36 cicc temps (`cccX8pB2.s`, `ccIjGSOW.s` = build artifacts of the 13:36 ninja) and the 13:07:07 `lh_{old,new}_keys.txt` (driver iter-16 scratch, already documented).
- Layer-hash dump state, re-counted this iteration (scratch `lh_*.iter39.txt`, paste-join in file order): both dumps 1152 tuples (mtimes still 12:52/12:54); **1088 differing, 64 matching; first differing tuple: line 9 = run=150 mix layer=4** (old h=`478a57cad0f3a017` vs new h=`4a44cf19f097a851`). Unchanged since iteration 15. Note these dumps are from the session's **last** AIME run (run 158, pre-13:36) — the in-flight rebuild suggests they may re-run and produce fresh dumps before committing.

## Serve state (do not touch)
- The live harness serve is the **manual** process (**pid 21537, up 05:16 at 13:37**) on GPU 0: `ninfer-serve models/qwen3_8_27b_nvfp4.ninfer --host 127.0.0.1 --port 8080 --max-context 262144 --kv-capacity 262144 --kv-dtype int8 --max-concurrency 2 --spec mtp --draft-tokens 3 --lm-head-draft` (lacks `--pending-timeout-ms 300000`). Supervisor `ninfer-serve` is STOPPED (since 08:30); `ninfer-serve-standby` STOPPED (since 08:44). `/v1/models` → 200 at 13:37.
- `/tmp/autoninfer-ops/pending.json` still holds `{"action":"restart-primary"}` (queued 08:45, deferred while interactive sessions are alive; re-verified 13:37). Expect the **supervisor** serve (wrapper flags incl. `--pending-timeout-ms 300000`) to come up once no interactive session is alive. Verify at start: `supervisorctl status ninfer-serve` + `curl -s http://127.0.0.1:8080/v1/models`. Do NOT re-queue it.
- GPU 1 HEALTHY per `bash tools/gpu_health.sh 1` at 13:37 (triad 1467.0 GiB/s pass, fma 110.69 TFLOP/s pass) — stamped this iteration even though no GPU work ran (backoff).

## The interactive session's WIP (as of 13:37 — temp artifacts stripped)
- `src/ops/linear/fp8/fp8_config.h`, `fp8_dispatch.cpp`, `src/ops/linear_add/fp8/fp8_linear_add_small_t.cu`, `src/ops/linear_swiglu/fp8/fp8_linear_swiglu_plan.cpp` — T=2..4 small-t schedules become the T=1 GEMV bit-clone (vpl=8, four interleaved accumulator chains, same pair-to-chain mapping); A8 parents (FP8 MLP gate/up) stay A8 at all T.
- `tests/ops/test_fp8_linear_t_parity.cpp` (new, the real test: T=1..4 per-column bit parity), `tests/CMakeLists.txt`, `tests/ops/linear/test_fp8_a16.cpp`, `tests/ops/linear/test_fp8_a8.cpp` (schedule expectations).
- **No longer in the WIP:** `NINFER_DEBUG_LAYERHASH` (stripped 13:36:44) and `test_tmp_binary_diff.cpp` (deleted 13:36:44). The diff is now exactly the fix + its test + updated schedule expectations.

## Next iteration, in order
1. **Check whether the interactive session is alive and whether it committed:** `ps -o pid,etime,cputime,stat -p 21662` (track only 21662; plus `ps aux | grep -w "pi"` — exclude the driver's own session via the parent chain: its grandparent is `drive.sh` under `supervisord`), then `git status --short`, `git log --oneline -3`, and `git diff | sha256sum` (**fingerprint was `7dade13f…fe151` at 13:37**; the 13:36 rebuild was in flight, so a commit or further edit may have landed). Also check for a build in flight: `ps aux | grep -E 'nvcc|ninja|cicc|ptxas' | grep -v grep` and `ls -lat /tmp | head -12` (a live build or fresh `cc*`/`tmpxft_*` compiler temps = check their parent process before calling them interactive writes).
   - **Interactive pi alive (especially with a dirty tree or an in-flight build) → back off exactly as iterations 8–39 did** (refresh this handover from fresh `/tmp` evidence — last-write scan `ls -lat /tmp | head -15` plus `find /tmp -maxdepth 1 -type f -newermt '2026-08-18 13:36:45' ! -name 'lh_*.iter*' ! -name 'lh_pasted.iter*' -printf '%T@ %p\n' | sort -n | tail` — interpret `cc*`/`tmpxft_*` compiler temp files as *build* artifacts and check their parent process before calling them interactive writes, `stat` the WIP files, re-count layer-hash diffs: `grep -oE 'LH run=[0-9]+ (mix|mlp) layer=[0-9]+ h=[0-9a-f]+' /tmp/lh_old.err` / same for `lh_new.err`, then `paste` the two extracts and `awk '$2!=$7 || $3!=$8 || $4!=$9 || $5!=$10 {d++} END{print d}'` — 1088/64 with first diff at line 9 = run=150 mix layer=4 (old `478a57cad0f3a017` vs new `4a44cf19f097a851`) is the current expectation if the dumps are unchanged — commit ONLY the handover + BLOCKERS state if it moved, push, end). Do not touch the tree. Driver scratch in /tmp (`lh_*.iterNN.txt`, `lh_pasted.iterNN.txt`) is not interactive work; check mtimes and process parents. If 21662's CPU time keeps climbing, that is activity (still back off while the tree is dirty — same rule).
   - **Committed → verify its commit:** contains the FP8 fix + `test_fp8_linear_t_parity`; does NOT contain `NINFER_DEBUG_LAYERHASH` or `test_tmp_binary_diff` (grep the new tree; both were stripped from the WIP at 13:36:44, so they should be absent). If they leaked in, record a BLOCKERS row; do not silently rewrite another session's commit. Then proceed to steps 2–5.
   - **Sessions gone, tree still dirty (abandoned WIP):** the driver may now take over — but treat the uncommitted diff as someone else's half-finished experiment: build it, run `test_fp8_linear_t_parity`, re-run the AIME k=0/k=1 diff (the `m_*.err` pattern / `matrix.sh` in /tmp), and only commit it if the evidence shows the fix is correct and the temp instrumentation is stripped (it is, as of 13:36:44); otherwise `git stash` it with a descriptive message and proceed to backlog #2, noting the stash in the handover.
2. **Clean rebuild** `cmake --build build -j` once the tree is clean/owned (build/ currently holds the WIP state, possibly mid-build).
3. **Measure at the new HEAD** (FP8 committed-column clone in): M1 menu command (k=3) — expect the accept rate to shift (compare to the attention fix's +8.2pp). New baseline row. Then the backlog **k=2 vs k=3 re-decision** (pre-fix A/B numbers are stale): same menu command with `--mtp-draft-tokens 2` vs `3`.
4. **Quality gate:** `tools/autoninfer/quality_gate.sh pre-<exp>` / `post-<exp>` around any further change; after the FP8 fix expect the k=0-vs-k=3 gate diff to shrink below the current 4/8 and the AIME k=1 idx-119 flip (280 vs 343) to disappear — verify by re-running the k=0 and k=1 AIME commands and diffing the token-id lines.
5. Backlog after that: prefill FP8 crossovers (backlog #2), host-side round overhead (backlog #5). **Web-research status (iter 17): all five backlog hypotheses already have concrete seeded entries in `docs/autoninfer/inspiration.md`** (H2's vLLM NVFP4 tile-sweep template is a ready-made experiment for backlog #2) — no new research needed before the next experiment; search only if a measurement is surprising.

## Budget note
Driver budget is 30 h wall clock from ~09:00 UTC (2026-08-18) → clean exit around ~15:00 UTC (~1 h 20 min remaining at 13:38). If the interactive session stays alive, remaining iterations continue to back off in docs-only mode (by design; the driver stops cleanly on budget, and `supervisorctl start autoninfer-driver` re-arms it).

## Do not repeat / do not touch
- The k=1 flip attribution — owned by the interactive session (FP8 linear small-t T=2..4 association + A16 island). Their evidence is in `/tmp` as listed above; their WIP was **not verified clean at completion of their last AIME run** (12:54: 1088/1152 layer-hash tuples still differ, first at line 9 = run=150 mix layer=4) — but they stripped the temp instrumentation and deleted the temp test at 13:36:44 and were rebuilding at 13:37, so a verified-clean commit may land any moment.
- Building/measuring/committing on the dirty tree while the interactive pi session (pid 21662) is alive.
- The manual serve (pid 21537) and GPU 0 in any way (bind/kill/restart/reconfigure).
- Starting the standby serve. Re-queueing `restart-primary` (already pending). Committing `.env` (git-ignored, EXA key).
- GPU 1: `bash tools/gpu_health.sh 1` before any benchmark (HEALTHY at 13:37). No GPU work ran this iteration (backoff #32).
- Speculative web-research surveys while blocked: the backlog is seeded; research only feeds a live decision.